hola que tal youtube videntes bienvenidos una vez más a una nueva entrega de este su curso de ahora con sql de cero en el día de hoy vamos a ver cómo trabaja la instrucción en build o nbl en ahora con sql la cual nos sirve para consultar valores nulos en una tabla cambiando por una descripción customizada para los ejemplos del día de hoy les voy a dejar en la descripción del vídeo el borrado y creación de nuestra tabla de productos a la cual vamos a reconfigurar para poder mostrar mejor los ejemplos así que vamos a copiar estas instrucciones de la descripción y las vamos a pegar en nuestro editor de código justo como lo vemos aquí posteriormente las ejecutamos y aquí confirmamos que se borró correctamente la tabla anterior de productos que teníamos y se creó nuestra nueva tabla se insertaron todos los registros correspondientes sin errores procedemos entonces a limpiar nuestra salida de consola y limpiamos nuestro editor de código hacemos un selecto a nuestra tabla de productos y aquí están los registros que tenemos si se fijan tenemos varios registros con el valor nulo vamos entonces hacer una consulta a nuestra nueva tabla de productos de forma que no nos traiga a estos valores null con esa descripción sino con una que nos haga la consulta mejor legible esto lo vamos a hacer con la instrucción en piel vamos a decirle entonces al sistema select vamos a seleccionar nuestro campo de producto como vamos a traer también el campo descripción pero en este caso vamos a colocar nbl abrimos paréntesis y ahora colocamos el campo de descripción posteriormente colocamos una coma y colocamos el texto sin descripción cerramos comillas cerramos paréntesis y debajo decimos que va a ser todo esto desde nuestra tabla productos punto y coma luego seleccionamos toda la consulta ejecutamos nuestra consulta nos trae un registro que dice sin descripción esto que estamos viendo aquí sin descripción son valores nulos dentro de nuestro campo descripción ya que es lo que configuramos en nuestra consulta seleccionan el campo a ir y producto y colocando nfl entre paréntesis colocamos nuestro campo de descripción y luego colocamos un texto que diga sin descripción esto que significa que esta instrucción va a tomar el campo descripción y todos los valores que sean nulos nos va a traer con el texto sin descripción todo esto de nuestra tabla de productos por eso cuando ejecuta la consulta vemos los registros normales y un registro que dice sin descripción aquí le podemos poner un alias a este campo que diga as descripción y ejecutamos de nuevo la consulta ahora vemos que el nombre del campo dice ya descripción lo que hace más legible la consulta en este caso estamos consultando en un campo cuyo tipo de dato es es decir estos valores son de tipo variar por lo tanto el valor que debemos traer en lugar de null es un texto pero qué pasaría si la consulta la hiciéramos a un campo con valores de tipo nombre o valores de tipo entero pues en este caso no podríamos traer el texto sin descripción o cualquier otro texto que le pongamos ya que esto no podría mostrarse en un campo de tipo de dato entero vamos a ver un ejemplo vamos a decir select haití producto vamos a colocar una coma luego vamos a traer el campo descripción descripción otra coma y ahora vamos a colocar n de l es decir consulta de los valores nulos dentro de paréntesis de el campo en este caso precio vamos a colocar una coma y entonces vamos a colocar entre comillas precio cerramos comillas cerramos paréntesis y decimos que haga todo esto desde nuestra tabla producto colocamos punto y coma y cuando ejecutamos qué sucede el sistema nos trae un error moral 0 17 22 que dice número inválido es decir lo que estamos intentando traer en el campo que nos va a generar la instrucción nbl es un valor de tipo string porque dice sin precio sin embargo el registro que se almacena en el campo precio es de tipo nombre por lo tanto no podemos colocar sin precio tenemos que colocar un valor numérico en este caso vamos a colocar un cero que estamos diciendo aquí que nos traigan los valores del campo precio y que cuando haya valores nulos en lugar de decir nos diga 0 seleccionamos y ejecutamos y aquí vemos entonces como el sistema nos trae todos los precios de los productos y donde hay valores nulos los trae con un 0 como lo vemos aquí aquí y acá en este caso los valores nulos del campo de descripción se están mostrando porque lo estamos consultando aquí normalmente del mismo modo colocaremos esta instrucción podríamos combinarla también y en lugar de descripción colocaremos entonces nbl descripción entre comillas sin descripción coma nbl precio coma cero cuando seleccionemos y ejecutemos entonces el sistema si nos va a traer el campo de descripción con el valor sin descripción donde haya valores nulos y el campo precio con valor cero donde hallaban los valores nulos tal como sólo estamos definiendo en la consulta si colocamos un alias descripción para el campo descripción y para la consulta de no valores nulos del campo precio decimos as precio cuando ejecutemos ahora si vemos que los nombres de los campos vienen más legibles y bien eso es todo por ahora referente a la cláusula nbl en ahora con sql muchas gracias por su atención nos vemos en una siguiente entrega hasta pronto