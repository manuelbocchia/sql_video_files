hola que tal youtube videntes bienvenidos a una nueva entrega de este curso de ahora con sql desde cero en el día de hoy vamos a continuar aprendiendo sobre las funciones internas de oracle anteriormente estuvimos viendo las funciones de string o de manejo de caracteres y ahora pasaremos pues a explorar y analizar las funciones matemáticas las funciones matemáticas demora con sql realizan operaciones con expresiones numéricas y siempre retornan un resultado con tipo de datos numéricos a estas funciones pues hay que pasarle un parámetro de entrada que sea de tipo numérico también para que retornen dichos valores vamos a empezar viendo la primera de estas funciones que es la función apps esta función lo que hace es que nos retorna el valor absoluto de un número que le pasemos en una ejecución si decimos por ejemplo select apps y le pasamos el número por ejemplo menos 50 cerramos paréntesis decimos que ejecutamos esto desde nuestra tabla actual que ya la conocemos cuando ejecutamos esta instrucción el sistema nos trae un 50 como número entero que representa el valor absoluto de menos 50 el resultado sería igual si colocaremos un signo de más si no colocamos signo alguno es exactamente el mismo resultado de igual modo la función apps calcula el valor absoluto de una operación si dijéramos por ejemplo aquí 10 + 10 y ejecutamos pues como el resultado es 20 pues el sistema trae el valor absoluto del resultado de esta operación vamos a colocar el valor original y ahora vamos a analizar otra función matemática conocida como 6 esta función 6 lo que hace es que redondea el valor de un número entero pero hacia arriba como es esto si decimos el excel y vamos a colocar un valor por ejemplo 15.50 cerramos paréntesis le decimos que ejecute todo esto desde nuestra tabla dual fíjense como la función 6 lo que hace es que redondea hacia 16 el punto 50 es decir redondea hacia arriba la función que realiza la operación contrario es la función flor la función flor lo que hace es que redondea el valor hacia abajo de una cantidad que le especificamos en una ejecución si decimos selector entre paréntesis le colocamos 13.30 como ejemplo from dual cuando ejecutamos el sistema lo que hace es entonces que redondea hacia abajo es decir a partir del punto 30 elimina el valor decimal pero hacia abajo o sea le resta a la cantidad decimal contrario a la función sail que suma la cantidad decimal sube hacia el siguiente valor entero continuamos viendo las funciones matemáticas internas de oracle vamos a ver ahora la función mod la función mod en ahora con lo que hace es que devuelve el residuo de la división entre dos números enteros si decimos select y le pasamos por ejemplo la división de 10 entre 3 cerramos paréntesis ejecutar desde la tabla dual cuando seleccionamos vemos que el residuo final de la división es 1 realiza la división de 10 entre 3 y tráeme el residuo utilizando la función mod esto es lo que inició esta instrucción y al final pues el resultado es 1 si en lugar de 3 colocaremos por ejemplo un 2 que es entonces como el resultado cambia a 0 ya que cero es el residuo de dividir 10 entre 2 otra función matemática de oracle es la función power qué es lo que hace la función power pues la función power realiza la operación de potenciación matemática si le dijéramos por ejemplo select power abrimos paréntesis colocamos un 2,3 cerramos paréntesis puntual cuando ejecutemos esta sentencia el sistema nos da 18 porque lo que hace la función power es que multiplica el primer valor que es tomado como base y el segundo valor que es tomado como exponente en matemática esto sería elevar 2 a la 3 dando como resultado 8 si quisiéramos por ejemplo calcular un 10 elevado a la 3 el resultado sería 1000 ejecuta la potenciación de 10 a la 3 seguimos analizando las funciones matemáticas de ahora con vamos a ver ahora la función round la función round ejecuta el redondeo de una cantidad en decimales en tantos lugares como se lo indiquemos en un segundo parámetro en una ejecución o si decimos select round abrimos paréntesis vamos a colocar la cantidad puntos 3 colocaríamos un punto 456 colocaríamos una coma y luego un 2 cerramos paréntesis le decimos que ejecute la operación utilizando la tabla dual aquí tenemos un error parece que a colocamos mal el nombre de la función es round ahora sí fíjense entonces como el sistema nos trae 123 punto 46 porque funciona aquí lo que hace es que cuando le pasamos luego de un punto analiza cuántos valores decimales hay después de ese punto y dependiendo la cifra que coloquemos acá en el tercer parámetro luego de la coma esa cantidad de lugares va a quitarle al decimal haciendo el redondeo correspondiente es decir 123.45 6 y decimos que haga el redondeo de dos lugares decimales en este caso sería 456 llevado a 46 si en lugar de un 2 le colocamos un 3 fíjense entonces como el sistema toma por completo los tres lugares decimales si en lugar de un 3 colocamos un 1 el sistema pues redondea a un solo decimal la cantidad luego del punto llevando el 456 a 5 si no le colocaremos ningún valor entero en el tercer parámetro de la ejecución el sistema lo que hace es que redondea toda la parte entera de la cifra vamos a quitar el tercer parámetro y ejecutamos fíjense cómo el sistema entonces elimina toda la parte decimal y deja únicamente la parte entera esto es si no le colocamos un tercer parámetro es decir si no le indicaremos qué cantidad decimales queremos redondear continuamos vamos a ver ahora la función 5 es una función que retorna la naturaleza de un número entero que le pasemos al sistema si colocamos select singh abrimos paréntesis y vamos a colocar un menos 100 cerramos paréntesis cuando ejecutemos esta instrucción nos coloca un -1 indicándonos que la naturaleza de la cifra es números negativos y colocaremos 100 solamente cuando ejecutemos trae un 1 positivo indicándonos que la cifra es de naturaleza vamos a ver ahora la siguiente función es la función trump esta función lo que hace es que cortar cifras decimales de una cantidad a diferencia de la función round que lo que hace es redondear es decir redondea el valor de decimales tanto como se lo indicamos en el parámetro la función tronco lo que hace es que corta la cantidad vamos a colocar un ejemplo decimos electrón abrimos paréntesis y colocamos la cifra 1234 vamos a colocar entonces un punto y vamos a decir igualmente uno dos tres cuatro vamos a colocar una coma y vamos a colocar un número tres cerramos paréntesis especificamos la tabla dual como siempre cuando ejecutamos la instrucción fíjense entonces como el sistema trae la cifra entera más uno dos tres como decimal esto es porque con este tres le estamos indicando que solamente queremos tres valores está ignorando el número cuatro es decir con este tres estamos seleccionando solamente estos tres valores para ser mostrados si colocamos un número cuatro cuando ejecutemos entonces si aparece realmente todas las cifras que pusiéramos un 1 solamente solamente veríamos el primer valor decimal que es el 1 como ven a diferencia de la función round que lo que hace es que redondea la cantidad de decimales que le indiquemos la función trump lo que hace es que corta la cantidad continuamos vamos a ver ahora la función score esta función realiza la operación de raíz cuadrada a una cantidad que le pasemos en una ejecución del sistema si decimos select escort abrimos paréntesis colocamos un número 9 cerramos paréntesis les decimos que haga todo utilizando la tabla dual al ejecutar la operación fíjense que trae el número 3 que es raíz cuadrada de 9 lugares de 9 colocamos 127 aquí vemos como trae 5 puntos y una increíble cantidad de decimales ya que es una raíz cuadrada inexacta para evitar que en una operación nos aparezcan todos estos decimales podríamos utilizar la función round junto con la funciones corp podríamos decirle select round abrimos paréntesis y al final de la función escorts cerramos el paréntesis cuando ejecutemos esto el sistema se lleva todos los decimales que vimos anteriormente esto que estamos viendo aquí son dos funciones anidadas se le llama funciones anidadas en ahora como le dijimos realiza el redondeo de la raíz cuadrada de 27 estos dos paréntesis pertenecen a la primera función y estos pertenecen a la función interna es el redondeo de una operación interna que está realizando el sistema y bien eso es todo por el momento respecto a las operaciones con funciones internas matemáticas en ahora con sql a dejar todas estas ejecuciones en la descripción del vídeo para que las copias las ejecuten y realicen más ejemplos con ellos seguiremos viendo las funciones de fechas y horas en oracle que son muy muy importantes así que nos vemos en una siguiente entrega gracias por su atención hasta pronto