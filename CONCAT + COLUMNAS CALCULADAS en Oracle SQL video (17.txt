hola que tal youtube videntes bienvenidos una vez más a este su canal informático freak a este curso decora con sql desde cero en el día de hoy vamos a aprender a cómo manejar columnas calculadas en sql anteriormente aprendimos que los operadores son símbolos que permiten realizar distintos tipos de operaciones los operadores relacionales o de comparación los aritméticos los de concatenación y los lógicos todos estos los iremos viendo más adelante a medida que vayamos entrando más en materia pero en el día de hoy pues empezaremos viendo cuáles son los operadores aritméticos en sql los operadores aritméticos nos permiten realizar cálculos con valores numéricos cálculos como multiplicación división suma y resta y es posible obtener salidas en las cuales una columna sea el resultado de un cálculo y no un campo de una tabla si nosotros quisiéramos por ejemplo ver los títulos precio y cantidad de cada libro en la tabla de libros que la creamos anteriormente pues la consulta sería la siguiente o sea una consulta especificando únicamente cuáles campos queremos consultar de la tabla de libros imaginen entonces que queremos calcular cuál es el precio de la cantidad total de un libro en específico que tenemos almacenado en una tabla pues podríamos hacer que ahora que haga el cálculo y lo incluya en una columna extra al momento de traer la consulta en una tabla ejecutando el cálculo directamente en el script como haríamos esto pues podríamos hacer la siguiente forma decimos selecciona el título el precio y la cantidad ponemos una coma y luego le especificamos que haga el cálculo de el precio de cada libro por la cantidad total existente todo este cálculo lo va a hacer en la tabla de libros ponemos punto y coma ejecutamos y fíjense entonces como el sistema nos trae justo lo que le pedimos el título el precio y la cantidad en sus columnas correspondientes y extra pues nos trae una columna calculando el precio por la cantidad de cada libro esto es fácilmente visible multiplicando precio por cantidad y aquí entonces en esta columna pues nos trae el resultado vamos a ver entonces otro ejemplo de columna calculada en hora con sql vamos a calcular el 10% de descuento en el costo de cada libro de la tabla como haríamos esto pues colocaríamos la instrucción selecciona título y precio y luego colocamos una coma y le decimos que nos calcula el precio colocando el nombre del campo precio - abrimos un paréntesis y le decimos que haga el cálculo de el precio por en este caso 0.1 cerramos paréntesis y le indicamos que este cálculo será hecho en la tabla libros colocamos un punto y coma y ejecutamos la instrucción y como vemos aquí pues el sistema nos trae el título que le pedimos el precio de cada libro y en una tercera columna nos ejecuta la operación que le indicamos en donde ejecutar un descuento del 10% colocando el precio de cada libro y restándole el valor que está en el campo precio por 0.1 todo esto desde la tabla libros fíjense entonces como aquí en una columna extra a cada libro le muestra el precio con un 10% de descuento en este caso lo que estamos haciendo es una consulta lo que indica que solamente estamos ejecutando la instrucción para ver cuál sería el cálculo al momento de ejecutar la consulta si quisiera y almacenar este descuento como un registro en la tabla podríamos hacerlo con la instrucción update y le diríamos actualizar en la tabla libros estableciendo en el campo precio y le decimos que lo que va a actualizar es el precio menos lo que sería el precio actual por 0.1 cerramos paréntesis y colocamos punto y coma ejecutamos la instrucción el sistema nos confirma que actualizo tres filas hacemos la consulta de la tabla para confirmar y aquí podemos ver la diferencia actualizada en donde se muestra que se aplica un descuento a cada libro que la tabla aclarando la instrucción que colocamos le dijimos actualiza en la tabla libros o sea haz una actualización en la tabla libros establece en el precio lo siguiente y colocando signo de igual le especificamos la operación para actualizar el descuento en el campo precio diciendo él coloca en precio el precio actual menos el precio por 0.1 lo que hace el sistema es que ejecuta primero lo que está dentro del paréntesis calculando cada precio por 0.1 y los resta del valor actual del campo precio y eso es lo que coloca en cada una de las filas y aquí vemos como efectivamente realizó la actualización vamos a ver ahora cómo realizar concatenación de datos en una tabla de sql una concatenación no es más que la unión de un registro de un campo con otro registro de otro campo de la misma tabla pero todo se reflejará en la consulta en un mismo campo vamos a ver un ejemplo de nuestra la tabla de libros digamos que vamos a decirle al sistema que nos traiga el título y que este título va a venir concatenado colocando estas dos barras verticales luego abrimos una comilla colocamos un guión central cerramos la comillas y colocamos de nuevo las dos barras verticales y colocamos el campo autor de nuestra tabla de libros luego le decimos al sistema que todo esto lo ejecute desde la tabla de libros seleccionamos y ejecutamos la instrucción fíjense entonces el sistema nos trae el título y el autor de los libros que tenemos en la tabla pero ambos unidos en un solo campo si lo hiciéramos individualmente lo haríamos de esta manera fíjense entonces como el sistema trae por un lado el título y por otro lado el autor pero ejecutando lo con la concatenación concatenando campos el sistema une estos dos valores en un solo campo y los muestran al traer el resultado de la consulta vamos a separar un poco más el guión dentro de la comida un espacio y luego al final otro espacio vamos a ejecutar de nuevo la instrucción fíjense entonces aquí como sistema une el campo título y el campo autor dentro del mismo campo esta doble barra vertical une el guión que colocamos aquí adentro junto con el campo autor ambos campos separados por el guión y colocaremos otro tipo de carácter aquí en lugar de guión digamos que vamos a colocar una barra vertical seleccionamos y ejecutamos fíjense cómo el sistema coloca aquí la barra vertical como elemento de separación porque se lo indicamos aquí selecciona el título une la barra vertical junto al campo autor de la tabla de libros y aquí está la consulta tal como se lo pedimos vamos usando esta misma regla de concatenación hacer un selecto en la tabla libros vamos a decirle select título colocamos doble barra vertical abrimos comillas vamos a colocar ahora un signo de dinero y vamos a colocar otra comillas luego entonces dos barras verticales otra vez para concatenar el campo precio le decimos que todo esto lo vamos a hacer igual desde nuestra tabla de libros seleccionamos y ejecutamos fíjense ahora como el sistema coloca el signo de dólar al principio de cada precio y lo trae concatenado junto al campo título como se lo dijimos acá selección al título concatenar lo consignó de dólar y al mismo tiempo con cadena con el campo precio en la tabla de libros y bien eso es todo por el momento respecto a columnas calculadas y concatenación de campos en ahora con la escuela continuaremos más adelante con las lecciones que tenemos preparadas para seguir avanzando en el curso gracias por su atención y hasta pronto